{
  "name": "open-thread",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An original mobile-first conversation card game PWA.",
  "engines": {
    "node": ">=20.19 <23"
  },
  "scripts": {
    "dev": "concurrently -k -n api,web \"node scripts/dev-server.mjs\" \"vite --host 0.0.0.0\"",
    "start": "node scripts/dev-server.mjs --production",
    "build": "tsc --noEmit && vite build",
    "test": "node --test",
    "check": "node --check game-engine.js && node --check adaptive-engine.js && node --check data/prompts.js && node --check data/date-rewards.js && node --check server/room-store.js && node --check server/app-server.js && node --check scripts/dev-server.mjs && tsc --noEmit && node --test"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "concurrently": "^9.2.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.14"
  }
}
