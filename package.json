{
  "name": "open-thread",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An original mobile-first conversation card game PWA.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test",
    "check": "node --check app.js && node --check game-engine.js && node --check competitive-engine.js && node --check data/prompts.js && node --check server/room-store.js && node --check server/app-server.js && node --check scripts/dev-server.mjs && node --test"
  }
}
